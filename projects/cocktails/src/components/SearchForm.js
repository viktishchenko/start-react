import React from "react";
import { useGlibalContext } from "../context";

const SearchForm = () => {
  return (
    <div>
      <h2>search form component</h2>
    </div>
  );
};

export default SearchForm;
